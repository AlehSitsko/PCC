# List of usernames including admin
usernames = [] # Empty list of usernames according to the task
# Check if the list is not empty
if len(usernames) != 0:
    print("We have users!")
else:
    # Expected output for empty list
    print("We need to find some users!")
